<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="commonSql">

	<!-- 공통코드 추가 -->
	<insert id="putCommon" parameterType="com.dongkeonoh.toybox.vo.CommonVo">
		INSERT INTO TB_COMMON
        VALUES 
     		 (
       		   #{com_id},
       		   #{com_category1},
       		   #{com_category2},
       		   #{com_category3},
       		   #{com_name},
       		   'Y'
     		 )
	</insert>
	
	<!-- 공통코드 추가시 번호 확인 -->
	<select id="getCommonNumber" resultType="String"  parameterType="com.dongkeonoh.toybox.vo.CommonVo">
		SELECT COM_ID
		  FROM (
				SELECT COM_ID
				  FROM TB_COMMON
				 WHERE COM_CATEGORY1 = #{com_category1}
				   AND COM_CATEGORY2 = #{com_category2}
				   AND COM_CATEGORY3 = #{com_category3}
				 ORDER BY COM_ID DESC
				)
		 WHERE ROWNUM = 1
	</select>
	
	<!-- 공통코드 목록 보기 -->
	<select id="getCommonList" resultType="com.dongkeonoh.toybox.vo.CommonVo"  parameterType="map">
		SELECT COMM.COM_ID, CAT1.CDE_CODE AS COM_CATEGORY1, CAT2.CDE_CODE AS COM_CATEGORY2, CAT3.CDE_CODE AS COM_CATEGORY3
		  FROM TB_COMMON COMM,
		       TB_CATEGORY CAT1,
		       TB_CATEGORY CAT2,
		       TB_CATEGORY CAT3
		 WHERE 1=1
		   AND COMM.COM_CATEGORY1 = CAT1.CDE_ID
		   AND COMM.COM_CATEGORY2 = CAT2.CDE_ID
		   AND COMM.COM_CATEGORY3 = CAT3.CDE_ID
		<if test='target == "category1"'>
		   AND CAT1.CDE_ID LIKE '%'||#{keyword}||'%'
		</if>
		<if test='target == "category2"'>
		   AND CAT2.CDE_ID LIKE '%'||#{keyword}||'%'
		</if>
		<if test='target == "category3"'>
		   AND CAT3.CDE_ID LIKE '%'||#{keyword}||'%'
		</if>
		<if test='target == "id"'>
		   AND COMM.COM_ID LIKE '%'||#{keyword}||'%'
		</if>
	</select>	
</mapper>

