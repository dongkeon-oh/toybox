<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="categorySql">

	<!-- 상위 카테고리 기반 카테고리 리스트 -->
	<select id="getCategory" resultType="com.dongkeonoh.toybox.vo.CategoryVo"  parameterType="com.dongkeonoh.toybox.vo.CategoryVo">
		SELECT CDE_ID, CDE_CODE
		  FROM TB_CATEGORY
		 WHERE CDE_UPPERID = #{cde_upperid}
	</select>	
	
	<!-- 카테고리 중복체크 -->
	<select id="getCategoryDuplication" resultType="int"  parameterType="com.dongkeonoh.toybox.vo.CategoryVo">
		SELECT COUNT(CDE_ID) AS CNT
		  FROM TB_CATEGORY
		 WHERE CDE_DEPTH = #{cde_depth}
           AND CDE_CODE = #{cde_code}
           AND CDE_UPPERID = #{cde_upperid}
	</select>
	
	<!-- 카테고리 추가 -->
	<insert id="putCategory" parameterType="com.dongkeonoh.toybox.vo.CategoryVo">
		INSERT INTO TB_CATEGORY
			 (
			   CDE_ID,
			   CDE_CODE,
       		   CDE_DETAIL,
       		   CDE_DEPTH,
       		   CDE_UPPERID
       		 )
        VALUES 
     		 (
     		   SEQ_COMMONDETAIL_ID.NEXTVAL,
       		   #{cde_code},
       		   #{cde_detail},
       		   #{cde_depth},
       		   #{cde_upperid}
     		 )
	</insert>	
	
	<!-- 카테고리 아이디로 카테고리 코드 조회 -->
	<select id="getCategoryCode" resultType="String"  parameterType="String">
		SELECT CDE_CODE
		  FROM TB_CATEGORY
		 WHERE CDE_ID = #{value}
	</select>
</mapper>

